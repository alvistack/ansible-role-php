---

# (c) Wong Hoi Sing Edison <hswong3i@pantarei-design.com>
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

- name: include release specific variables
  include_vars: "{{ loop_var }}"
  with_first_found:
    - files:
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_os_family | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}.yml"
        - "{{ ansible_os_family | lower }}.yml"
      paths: "../vars"
      skip: "yes"
  loop_control:
    loop_var: "loop_var"
  tags: php

- name: include release specific tasks
  include_tasks: "{{ loop_var }}"
  with_first_found:
    - files:
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_os_family | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}.yml"
        - "{{ ansible_os_family | lower }}.yml"
      paths: "."
      skip: "yes"
  loop_control:
    loop_var: "loop_var"
  tags: php

- name: install composer globally
  shell: |
    curl -skL https://getcomposer.org/download/1.7.2/composer.phar > $COMPOSER_BIN_DIR/composer
    chown root:root $COMPOSER_BIN_DIR/composer
    chmod 777 $COMPOSER_BIN_DIR/composer
    mkdir -p $COMPOSER_HOME
    chown root:root $COMPOSER_HOME
  args:
    creates: "/usr/local/bin/composer"
    warn: false
  environment:
    COMPOSER_HOME: "/usr/local/share/composer"
    COMPOSER_BIN_DIR: "/usr/local/bin"
  tags: php

- name: composer self-update
  shell: |
    composer self-update --no-interaction
  args:
    warn: false
  register: composer_self_update_result
  changed_when: "'You are already using composer' not in composer_self_update_result.stderr"
  tags: php

- name: composer global init
  shell: |
    composer global init --no-interaction
  args:
    creates: "/usr/local/share/composer/composer.json"
    warn: false
  register: composer_global_init_result
  changed_when: composer_global_init_result.rc != 0
  environment:
    COMPOSER_HOME: "/usr/local/share/composer"
    COMPOSER_BIN_DIR: "/usr/local/bin"
  tags: php

- name: composer global require
  shell: |
    composer global {{ item.state }} {{ item.name }} --no-interaction
  args:
    warn: false
  with_items:
    - { state: 'require', name: 'hirak/prestissimo:@stable' }
    - { state: 'require', name: 'consolidation/cgr:@stable' }
  register: composer_global_require_result
  changed_when: composer_global_require_result.rc != 0
  environment:
    COMPOSER_HOME: "/usr/local/share/composer"
    COMPOSER_BIN_DIR: "/usr/local/bin"
  tags: php

- name: composer global update
  shell: |
    composer global update
  args:
    warn: false
  register: composer_global_update_result
  changed_when: composer_global_update_result.rc != 0
  environment:
    COMPOSER_HOME: "/usr/local/share/composer"
    COMPOSER_BIN_DIR: "/usr/local/bin"
  tags: php

- name: update /etc/profile.d/composer.sh
  lineinfile:
    path: "{{ item.path }}"
    regexp: "{{ item.regexp }}"
    line: "{{ item.line }}"
    insertafter: "EOF"
    state: "present"
    create: "yes"
  with_items:
    - { path: "/etc/profile.d/composer.sh", regexp: "^export COMPOSER_HOME=", line: "export COMPOSER_HOME=/usr/local/share/composer" }
    - { path: "/etc/profile.d/composer.sh", regexp: "^export COMPOSER_BIN_DIR=", line: "export COMPOSER_BIN_DIR=/usr/local/bin" }
    - { path: "/etc/profile.d/composer.sh", regexp: "^export CGR_BIN_DIR=", line: "export CGR_BIN_DIR=/usr/local/share/composer" }
  tags: php

- name: cgr require
  shell: |
    cgr {{ item.state }} {{ item.name }}
  with_items: "{{ _cgr }}"
  register: cgr_require_result
  changed_when: cgr_require_result.rc != 0
  environment:
    COMPOSER_HOME: "/usr/local/share/composer"
    COMPOSER_BIN_DIR: "/usr/local/bin"
    CGR_BIN_DIR: "/usr/local/bin"
  tags: php

- name: cgr update
  shell: |
    cgr update
  register: cgr_update_result
  changed_when: "'Package operations:' in cgr_update_result.stderr"
  environment:
    COMPOSER_HOME: "/usr/local/share/composer"
    COMPOSER_BIN_DIR: "/usr/local/bin"
    CGR_BIN_DIR: "/usr/local/bin"
  tags: php
