---

# (c) Wong Hoi Sing Edison <hswong3i@pantarei-design.com>
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

- name: include default variables
  ansible.builtin.include_vars: "../vars/main.yml"

- name: include release specific variables
  ansible.builtin.include_vars: "{{ _loop_var }}"
  loop: "{{ query('first_found', _params) }}"
  loop_control:
    loop_var: _loop_var
  vars:
    _params:
      files:
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}.yml"
        - "{{ ansible_os_family | lower }}-{{ ansible_distribution_version | lower }}.yml"
        - "{{ ansible_os_family | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_os_family | lower }}.yml"
      paths:
        - "../vars"
      skip: true

- name: include release specific tasks
  include_tasks: "{{ _loop_var }}"
  loop: "{{ query('first_found', _params) }}"
  loop_control:
    loop_var: _loop_var
  vars:
    _params:
      files:
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_distribution | lower }}.yml"
        - "{{ ansible_os_family | lower }}-{{ ansible_distribution_version | lower }}.yml"
        - "{{ ansible_os_family | lower }}-{{ ansible_distribution_major_version | lower }}.yml"
        - "{{ ansible_os_family | lower }}.yml"
      paths:
        - "."
      skip: true

- name: prepare directories
  ansible.builtin.file:
    dest: "{{ item.dest }}"
    owner: "{{ item.owner | default('root') }}"
    ansible.builtin.group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0755') }}"
    state: "{{ item.state | default('directory') }}"
  loop:
    - { dest: "/etc/profile.d" }
    - { dest: "/usr/local/share/composer" }

- name: download archive
  ansible.builtin.shell: |
    set -euxo pipefail
    mkdir -p /var/log/ansible
    exec &> >(tee -a /var/log/ansible/{{ composer_download.checksum }}-get_url.log)
    TMP_DIR="$(mktemp -d)" \
      && cd $TMP_DIR \
      && mkdir -p .{{ composer_download.dest | dirname }} \
      && curl -skL -m 300 {{ composer_download.url }} > .{{ composer_download.dest }} \
      && echo -n "{{ composer_download.checksum }} .{{ composer_download.dest }}" | sha256sum -c - \
      && mkdir -p {{ composer_download.dest | dirname }} \
      && mv .{{ composer_download.dest }} {{ composer_download.dest }} \
      && rm -rf $TMP_DIR
  args:
    executable: "/bin/bash"
    creates: "/var/log/ansible/{{ composer_download.checksum }}-get_url.log"
  register: result
  until: result is succeeded

- name: unarchive package
  ansible.builtin.shell: |
    set -euxo pipefail
    mkdir -p /var/log/ansible
    exec &> >(tee -a /var/log/ansible/{{ composer_download.checksum }}-unarchive.log)
    cd /usr/local/bin
    TMP_DIR="$(mktemp -d)" \
      && cp {{ composer_download.dest }} $TMP_DIR/composer \
      && chown root:root $TMP_DIR/composer \
      && chmod 0755 $TMP_DIR/composer \
      && mv $TMP_DIR/composer . \
      && rm -rf $TMP_DIR
  args:
    executable: "/bin/bash"
    creates: "/var/log/ansible/{{ composer_download.checksum }}-unarchive.log"

- name: copy templates
  ansible.builtin.template:
    dest: "{{ item.dest }}"
    src: "{{ item.src | default('./templates' + item.dest + '.j2') }}"
    owner: "{{ item.owner | default('root') }}"
    ansible.builtin.group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0644') }}"
  loop:
    - { dest: "/etc/profile.d/composer.sh" }

- name: prepare files
  ansible.builtin.file:
    dest: "{{ item.dest }}"
    owner: "{{ item.owner | default('root') }}"
    ansible.builtin.group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0644') }}"
    state: "{{ item.state | default('file') }}"
  loop:
    - { dest: "/usr/local/bin/composer", mode: "0755" }

- name: composer global require
  environment:
    PATH: "/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
  ansible.builtin.shell: |
    set -euxo pipefail
    source /etc/profile.d/composer.sh
    composer --no-interaction global {{ item.state }} {{ item.name }}
  args:
    executable: "/bin/bash"
  loop:
    - { state: 'require', name: 'alvistack/cgr:@dev' }
  register: result
  until: result is succeeded
  changed_when: result is not succeeded

- name: composer global update
  environment:
    PATH: "/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
  ansible.builtin.shell: |
    set -euxo pipefail
    source /etc/profile.d/composer.sh
    composer --no-interaction global update
  args:
    executable: "/bin/bash"
  register: result
  until: result is succeeded
  changed_when: result is not succeeded

- name: cgr require
  environment:
    PATH: "/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
  ansible.builtin.shell: |
    set -euxo pipefail
    source /etc/profile.d/composer.sh
    cgr {{ item.state }} {{ item.name }}
  args:
    executable: "/bin/bash"
  loop: "{{ cgr }}"
  register: result
  until: result is succeeded
  changed_when: result is not succeeded

- name: cgr update
  environment:
    PATH: "/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
  ansible.builtin.shell: |
    set -euxo pipefail
    source /etc/profile.d/composer.sh
    cgr update
  args:
    executable: "/bin/bash"
  register: result
  until: result is succeeded
  changed_when: "'Package operations:' in result.stderr"

- name: flush handlers
  ansible.builtin.meta: flush_handlers

- name: systemctl start php-fpm.service
  ansible.builtin.service:
    name: "{{ _service[php_release] }}"
    state: "started"
    enabled: true
  changed_when: false
  failed_when: false
